maven is toll is used to building of the java -based project

- after building the code the it as converted into teh artifacts
- final exceutable code
- artifacts are the files which are build during the build
- the maven ,gradle,ant
- go inside teh maven after building the java code ,you will find the pom.xml
- how to build the
- hoe to build the project is expalined in the pom.xml file.

* maven how to build the project os shownn in the pim.xml
* DEFAULT LIFE CYCLE PHASES:
* validate-> chcke if the project structure and required information are correct.
* compile-> compile the source code unto byte code.
* test -> run test using the frameworks like junit.
* package-> bundle compile code into distributed format
* verify->run checks to ensure package meets quality standars
* install-> place a package into the loacl maven repository for use in other projects
* deploy->copy the package to a remote repository for sharing with other devopers
  the entire code is built in .war,.jasr and all
  - artifacts are teh final executable package of the code.
  * executable code
  * maven
    \*install the maven
  * if teh java project is buid using the maven then it is containd the pom.xml file
  * project object model .xml file
  * .jar is for simple applications
  * .war is for large applicxations
  * how to build the projecy os instraucted in the pom.xml file
  * which java versions to use which additoanl pgins to use(additional softwares)\

* where there is the pom.xml is tehre theerw we can give only 7 commands
* validate
* compie
* test
* pacakge
* verify
* insrtall deployemnt

* mvn validate(check evrything ais there or not)
* mvn test
* mav pacakge
* mvn deplymnet
* mvn test
* mvn compile(java language compile 0 or 1 )
* mvn pacakge(war files is there)
