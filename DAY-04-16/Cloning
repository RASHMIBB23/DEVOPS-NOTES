suod install yum -y install

- then connect to th eec2 instances
- after adding the collaboraters the smae code id you ahve to paste and continue further
- after adding go to the collaborator and do it
- why the does the pull request is done

* is to make the code changes to review and a=safe merging into the main branch
* safe merging into the main branch
* create the new barnch and works on it and do the merge the branch on it

```bash
#!/bin/bash
set -euo pipefail

if ! command -v aws >/dev/null 2>&1; then
    echo "Error: aws CLI is not installed."
    exit 1
fi

echo "Listing S3 buckets..."
aws s3api list-buckets --query "Buckets[].Name" --output text
```

- what do u mean the commit amend
- it means the modifying the lastest commit instaed of creating the new one

* for the direct authentication
* git clone ausername:user token @ github gmail.com
* while cloning onlywe have given the authentication so it is not asking
* there is anothe rname called git tage
  \*tags are the set of names given to different versions of files
* name given to teh certain version of files
* git push origin tagename

---

branching startergy

- manager created the fature branch and the commited things ae come intro the fature1.
- it doesnpt pusointo the main barch it is a separate branch

* if all the code work sfine ,all the test cases passes and the commits

---

git fetch -> it downloads teh code from th remote repository.
git pull-> it download the code from the local repository and the merge the repository
what is the purpose of the pull request

- it helps of the pr merge
- its heple to write the code ,before merging we can able to discuss teh code
- gitignore is a file
  some enviromental fikes is tored tehre
  what is git stah??
  -used for uncommited work.
  - to save the uncommited code and suddenly you want to shift the branches for tht u have to use
  * what is teh difference between the git reset and git revert->
  * git reset-> moves to the main branch
  * git revert->
    revert created branch new commit this commit changes only gets chenged from thr preevius comment
    ---
    ---
    Default branch (main): The primary branch shown when someone visits the repo; base for new pull requests.

Feature branches: Created from main to develop new features or fixes in isolation.

Pull requests (PRs): Used to merge changes from a branch into main with code review.

Branch protection rules: Prevent direct pushes, enforce PR reviews, CI checks, or require status checks before merging.

Merge methods: GitHub supports merge commits, squash merges, and rebase merges, depending on your workflow
